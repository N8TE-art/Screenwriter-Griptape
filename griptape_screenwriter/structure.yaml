version: 1.0
runtime: python3
runtime_version: 3.11 # Using Python 3.10 environment
build:
  requirements_file: requirements.txt
  cache_build_dependencies:
    enabled: false       # disable caching to ensure fresh installs (reproducibility)
    watched_files:
      - requirements.txt
run:
  main_file: structure.py
